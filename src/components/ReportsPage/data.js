export const data = [
  {
    id: 0,
    name: "Asset deviating most from FDS (Over working)",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 1,
    name: "Asset deviating most from FDS (Under delivering from flow perspective)",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 2,
    name: "Assets working at peak condition",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 3,
    name: "Asset Behaviour Summary, top sub assets that get discussed the most",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 4,
    name: "Asset Behaviour Summary, top issues that get discussed the most",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 5,
    name: "Oscillation/ Vibration score for all Fans (Supply, Extract, Return)",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 6,
    name: "Assets operating In Hand",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 7,
    name: "Asset deviating most from FDS (Over working)",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 8,
    name: "Asset Behaviour Summary, top issues that get discussed the most",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 9,
    name: "Critical Assets - Urgent Attention Required",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 10,
    name: "Assets with Unusual Energy Consumption Patterns",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 11,
    name: "Assets Showing Signs of Deterioration",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 12,
    name: "Assets Requiring Regular Maintenance",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 13,
    name: "Assets with Unpredictable Behavior",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 14,
    name: "Assets with Inconsistent Performance",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 15,
    name: "Assets Showing Early Signs of Failure",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 16,
    name: "Assets Operating at Peak Efficiency",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 17,
    name: "Assets with Low Utilization Rates",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 18,
    name: "Assets Requiring Calibration",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 19,
    name: "Assets with High Maintenance Costs",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 20,
    name: "Assets with Low Energy Consumption",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 21,
    name: "Assets Requiring Immediate Maintenance",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 22,
    name: "Assets Performing Beyond Expectations",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 23,
    name: "Assets with High Operational Efficiency",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 24,
    name: "Assets Undergoing Scheduled Maintenance",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 25,
    name: "Assets with Predictable Behavior",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 26,
    name: "Assets Operating at Optimal Performance",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 27,
    name: "Assets with Minimal Downtime",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 28,
    name: "Assets Requiring Inspection",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 29,
    name: "Assets Showing Signs of Wear and Tear",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 30,
    name: "Assets with High Reliability",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 31,
    name: "Assets Requiring Troubleshooting",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 32,
    name: "Assets Undergoing Maintenance Checks",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 33,
    name: "Assets with Stable Performance",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 34,
    name: "Assets with Consistent Output",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 35,
    name: "Assets with Low Failure Rate",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 36,
    name: "Assets Requiring Upgrades",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 37,
    name: "Assets with Longevity",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 38,
    name: "Assets Undergoing Performance Testing",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
  {
    id: 39,
    name: "Assets with Enhanced Efficiency",
    fds_value: generateRandomData(),
    avg_value: generateRandomData(),
    diff_value: generateRandomData(),
    lifecycle: Array.from({ length: 9 }, (_, index) => ({
      value: generateRandomValue(),
      name: `SubAsset-${index + 1}`,
    })),
  },
];

// Function to generate random data
function generateRandomData() {
  return Array.from({ length: 9 }, () => Math.floor(Math.random() * 10) + 1);
}
function generateRandomValue() {
  return Math.floor(Math.random() * 10) + 1;
}
